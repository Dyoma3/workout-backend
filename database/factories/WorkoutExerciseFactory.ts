import WorkoutExercise from 'App/Models/WorkoutExercise';
import Factory from '@ioc:Adonis/Lucid/Factory';

export default Factory.define(WorkoutExercise, ({ faker }) => {
  return {
    //
  };
}).build();
